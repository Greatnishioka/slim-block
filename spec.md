# 4方向重力スライムパズル
## MVP実装仕様書

## 1. ゲーム概要

プレイヤーが箱の内部に働く**重力方向を上下左右の4方向から選択**し、マップ内に散らばったスライムを移動・融合させながら、回収口まで運ぶパズルゲーム。

操作対象はスライムそのものではない。

プレイヤーが操作できるのは、

- 上重力
- 下重力
- 左重力
- 右重力

の4種類のみ。

重力を変更すると、マップ内のすべてのスライムが同時にその方向へ移動する。

スライム同士が接触すると一つの大きな塊として結合する。

大きな塊を維持したまま回収口へ到達させるほど高得点となる。

ただし、大きなスライムがマップ内の障害物へ衝突すると、その障害物を境界としてスライムが分断される場合がある。

ゲームの基本的なリスク・リターンは、

> 大きくまとめるほど高得点になるが、大きくなるほど地形によって分断されやすい

という構造とする。


# 2. MVPの設計方針

最初のバージョンでは機能を極力増やさない。

プレイヤーが覚える必要のある操作は、

> **重力方向を選択する**

だけとする。

以下はMVPでは実装しない。

- キャラクター固有能力
- アイテム
- HP
- 敵
- 時間制限
- スキル
- スライムの種類
- ステージ内スイッチ
- 扉
- 複雑な物理演算
- スライム個別操作

ゲームの深さは基本的に、

- スライムの初期配置
- 壁
- 内部障害物
- 回収口

の配置によって作る。


# 3. 基本ゲームループ

1. ステージ開始
2. マップ内に複数のスライムが配置される
3. プレイヤーが重力方向を選択する
4. 全スライムがその方向へ移動する
5. スライム同士が接触した場合は融合する
6. 障害物によって連結が切れた場合は分裂する
7. 回収口に接触したスライム塊を回収する
8. 回収した塊のサイズからスコアを計算する
9. 未回収スライムが残っていれば次の入力を待つ
10. 全スライムを回収したらステージクリア


# 4. 操作

操作は4方向のみ。

PCでは以下を想定する。

- ↑ / W：上方向へ重力
- ↓ / S：下方向へ重力
- ← / A：左方向へ重力
- → / D：右方向へ重力

画面上にも4方向のボタンを配置する。

例：

```text
        ↑

     ←  ●  →

        ↓
```

スマートフォンでは画面上のボタンをタップして操作できるようにする。

同時入力は不要。

1回の入力によるスライム移動が完全に終了するまで、次の入力は受け付けない。


# 5. マップ

マップは2Dグリッドとして管理する。

MVPでは例として、

- 10 × 10
- 12 × 12
- 16 × 12

程度の小規模マップを想定する。

各セルは最低限、以下のいずれかの状態を持つ。

```text
EMPTY
WALL
OBSTACLE
DRAIN
```

スライムはマップとは別レイヤーで管理する。


# 6. 壁

壁はスライムを完全に停止させる。

壁を通過することはできない。

マップ外周についても壁として扱う。

```text
████████████
█          █
█          █
█          █
████████████
```

壁自体にはスライムを分裂させる特殊効果は持たせない。


# 7. スライム

## 7.1 基本単位

スライムは「1個のキャラクター」ではなく、**複数のスライムセルの集合**として表現する。

例：

```text
●
```

は体積1。

```text
●●
●●
```

は体積4。

```text
●●●●●
●●●●●
●●●●●
●●●●●
●●●●●
```

は体積25。


## 7.2 塊の判定

上下左右のいずれかで接続しているスライムセルを、同一の「スライム塊」とする。

斜め方向だけで接しているセルは同じ塊として扱わない。

例：

```text
●●
 ●
```

上下左右で接続しているため1塊。

一方、

```text
●
 ●
```

は2塊。


# 8. スライムの融合

別々のスライム塊が移動によって接触し、上下左右に連結した場合、自動的に一つの塊として扱う。

特別な融合操作は存在しない。

```text
● → ← ●
```

接触後：

```text
●●
```

融合によってセル数が失われることはない。

例：

3セル + 5セル → 8セル


# 9. スライムの移動

プレイヤーが重力方向を変更すると、すべてのスライムセルがその方向へ移動する。

例：

```text
重力 ↓

●

↓

●

↓

████
```

壁または移動不能な地形に到達するまで移動する。

プレイヤーから見ると連続的に落下しているようにアニメーションさせるが、ゲームロジック自体はグリッド単位で処理してよい。


# 10. 内部障害物

内部障害物はスライムの流れを分岐させるために使用する。

例：

```text
重力 ↓

●●●●●
●●●●●
●●●●●

  █
  █
  █
```

スライムが障害物へ流れ込んだ場合、障害物の左右へ回り込むようにスライムセルが移動する。

結果として、

```text
●● █ ●●
●● █ ●●
●● █ ●●
```

のような状態になる可能性がある。

障害物によってスライムセル間の上下左右の連結が完全に失われた場合、その時点で別々のスライム塊として扱う。

つまり「分裂」という専用処理を発動するのではなく、

> **移動後のセル集合に対して連結成分を再計算する**

ことで分裂を表現する。


# 11. 分裂

例として12セルの塊が障害物によって、

```text
●● █ ●●
●● █ ●●
●● █ ●●
```

となった場合、

左6セルと右6セルの間に上下左右の接続が存在しないため、

```text
6セル + 6セル
```

の2つの独立した塊になる。

その後はそれぞれ別々に移動・融合・回収できる。


# 12. 回収口

マップ内には最低1つの回収口を配置する。

例：

```text
◎
```

スライム塊の一部が回収口に到達した場合、**その塊全体を回収対象とする。**

例：

```text
●●●
●●●
  ●
  ↓
  ◎
```

回収開始後、塊全体をマップから削除する。

ゲームロジック上は即時削除でもよいが、表示上は吸い込みアニメーションを再生する。


# 13. 吸い込み演出

このゲームにおける主要な報酬演出とする。

小さい塊：

```text
● → ◎
```

短く吸い込まれる。

大きな塊：

```text
●●●●●
●●●●●
●●●●●
   ↓
   ◎
```

スライム全体が引き伸ばされながら回収口へ吸い込まれる。

塊が大きいほど、

- 吸い込み時間を若干長くする
- スライムの変形を大きくする
- 効果音を強くする
- 軽い画面振動を追加する
- 獲得スコアを大きく表示する

など、演出を強化する。

ただしMVPでは簡易アニメーションでもよい。


# 14. スコア

一度に回収したスライム塊が大きいほど高得点になる。

MVPでは以下を基本式とする。

```text
score = 100 × n²
```

`n` は回収した塊に含まれるスライムセル数。

例：

```text
1セル  = 100
2セル  = 400
3セル  = 900
5セル  = 2,500
10セル = 10,000
25セル = 62,500
```

この方式により、同じ総量のスライムでも分割して回収するより、一つの巨大な塊として回収する方が高得点になる。

例：

10セルを一括回収：

```text
100 × 10² = 10,000
```

5セルずつ2回：

```text
100 × 5² + 100 × 5²
= 5,000
```

したがって、

> 大きく育てるほど得点が高い  
> しかし大きくなるほど地形によって分断されやすい

というゲーム性を成立させる。


# 15. クリア条件

ステージ内に存在するすべてのスライムセルを回収するとクリア。

スコアはクリア条件に含めない。

そのため初心者は、

> 小さな塊を少しずつ回収して安全にクリア

できる。

上級者は、

> 可能な限り一つの巨大な塊へ融合させてから回収

することで高得点を狙える。


# 16. 手数

MVPではゲームオーバー条件として使用しない。

ただし重力方向を変更した回数を記録する。

```text
MOVES: 7
SCORE: 12500
```

程度の表示を行う。

将来的には、

- 最少手数
- ハイスコア
- ランク

などへ利用可能。


# 17. 画面構成

最低限以下を表示する。

```text
SCORE  12,500
MOVES  7

        ↑
        ○

┌────────────────┐
│ ●●      █      │
│ ●       █      │
│                │
│      ███       │
│           ●●   │
│             ◎  │
└────────────────┘

○ ←     ↓     → ○
```

重要なのはゲーム盤面なので、UIは極力小さくする。


# 18. ステージデータ

ステージはコードへ直接書かず、JSON等のデータとして定義できる構造にする。

概念例：

```json
{
  "width": 10,
  "height": 10,
  "walls": [],
  "obstacles": [],
  "drains": [],
  "slimes": []
}
```

これによりステージ追加時にゲームロジックを変更する必要がないようにする。


# 19. MVPステージ案

最初は5ステージ程度でよい。

Stage 1：
重力操作と回収を理解する。

Stage 2：
複数スライムを融合させる。

Stage 3：
小さな障害物を追加する。

Stage 4：
巨大化すると障害物によって分断される構造を体験する。

Stage 5：
「小さい状態で危険地帯を通過させ、回収口付近で巨大化する」ことが高得点につながるステージ。


# 20. 技術構成

## 推奨：Web

MVPではWeb実装を第一候補とする。

推奨構成：

```text
TypeScript
+
HTML/CSS
+
Canvas 2D
```

ゲームエンジンは必須ではない。

Reactも必須ではない。

ゲーム盤面をCanvasで描画し、スコアや操作UIのみHTMLで構築する程度で十分。

理由：

- 配布が容易
- ビルドが軽い
- AIコーディングエージェントで生成・修正しやすい
- 2Dグリッドゲームなので高度なゲームエンジンが不要
- スマートフォンでも動作させやすい
- インストール不要
- GitHub Pages等でも公開可能


# 21. 物理エンジンについて

MVPではMatter.js等の物理エンジンは使用しない。

今回必要なのは現実の流体シミュレーションではなく、

> 「プレイヤーが結果を予測できるスライム」

である。

したがって、決定論的なグリッド処理を優先する。

同じ盤面で同じ方向を入力した場合は、必ず同じ結果になること。


# 22. 実装上の最重要ポイント

最大の技術検証ポイントは、

> **巨大なセル集合が障害物へ衝突した際に、直感的に「流れて・分かれて・再結合する」挙動を作れるか**

である。

そのため、最初からUIやステージを作り込まない。

最初のPoCでは、

```text
20 × 20程度のグリッド
1種類のスライム
1本の縦長障害物
4方向重力
```

だけを作る。

以下の挙動を確認する。

```text
■■■■■
■■■■■
■■■■■
   ↓
   █
   █
   █
```

が、

```text
■■ █ ■■
■■ █ ■■
■■ █ ■■
```

のように自然に分かれること。

その後、重力を横方向へ変更した際に再び融合できること。

このPoCが成立してから、

- 回収口
- スコア
- ステージ
- 演出
- UI

を追加する。


# 23. MVP完成条件

以下がすべて動けばMVP完成とする。

- 4方向へ重力を変更できる
- スライムが重力方向へ移動する
- スライムが実際の面積を持つ
- スライム同士が接触すると融合する
- 障害物によってスライムが分裂する
- 分裂したスライムが再融合できる
- 回収口へ到達すると塊全体が回収される
- 塊の大きさに応じてスコアが増える
- 全スライム回収でステージクリア
- ステージをデータから読み込める

それ以外の機能はMVP完成後に検討する。


# 24. ゲームの一文説明

> **重力を上下左右に切り替えてスライムを集め、なるべく大きな塊のまま回収口へ運ぶパズルゲーム。**

ゲームの基本思想は、

> **操作は単純、結果は地形によって複雑になる。**

とする。
